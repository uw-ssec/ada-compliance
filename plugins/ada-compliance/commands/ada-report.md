---
name: ada-report
description: Emit the compliance matrix from the most recent audit in Markdown and CSV formats.
---

Using the compliance-matrix-template skill, emit the compliance matrix from the most recent audit in both Markdown table and CSV formats. If no audit has been run in this session, prompt the user to run /ada-audit first.
