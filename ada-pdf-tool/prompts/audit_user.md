You are receiving a structured JSON extraction from a PDF document produced by the docling library.

Apply every WCAG 2.1 AA rule from your system prompt to every element in this extraction.
Classify each element as auto-fix, human-review, preserve, or info per the rules.
Return only the JSON object — no prose, no markdown.

Extraction:

{{EXTRACTION_JSON}}
